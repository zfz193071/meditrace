# 服务重启记录

## 2026-08-28 服务重启

### 问题描述
前端服务启动时端口 3000-3002 均被占用，导致服务在 3003 端口启动。

### 解决步骤

1. **检查占用端口的进程**
   ```bash
   lsof -ti:3000  # 返回: 44340
   lsof -ti:3001  # 返回: 41474
   lsof -ti:3002  # 返回: 41575
   lsof -ti:3003  # 返回: 44388
   ```

2. **清理端口**
   ```bash
   kill -9 44340 41474 41575 44388
   ```

3. **重启服务**
   - 后端服务：已在 8000 端口正常运行
   - 前端服务：成功在 3000 端口启动

### 当前服务状态

| 服务 | 端口 | 状态 | 访问地址 |
|------|------|------|----------|
| 后端 (FastAPI) | 8000 | ✅ 运行中 | http://localhost:8000 |
| 前端 (Next.js) | 3000 | ✅ 运行中 | http://localhost:3000 |

### 服务启动命令

**后端服务**:
```bash
cd src/backend && source venv/bin/activate && python main.py
```

**前端服务**:
```bash
cd src/frontend && pnpm dev
```

### 注意事项
- 如果再次遇到端口占用问题，重复上述清理步骤
- 可以使用 `lsof -ti:PORT | xargs kill` 一键清理单个端口
