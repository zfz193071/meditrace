# Chat 页面标题动态管理 - 测试指南

## 📋 功能概述

实现类似 Doubao 聊天页面的标题动态管理：
1. **URL 无 ID** → 显示"新的对话"
2. **URL 有 ID** → 显示对应对话的标题
3. **发送第一条消息** → 标题自动更新为消息摘要

---

## 🧪 测试场景

### 场景 1: 访问 `/chat`（无 ID，新对话）

**步骤:**
1. 确保前端服务运行在 `http://localhost:3000`
2. 在浏览器中访问 `http://localhost:3000/chat`
3. 观察页面标题（顶部导航栏）

**预期结果:**
- ✅ 标题显示为 "新的对话" 或 "新的诊断对话"
- ✅ 页面自动创建一个新对话
- ✅ 左侧对话列表显示该对话

---

### 场景 2: 访问 `/chat?id=xxx`（有 ID，加载已有对话）

**步骤:**
1. 先在左侧对话列表中选择一个对话，记住其 ID（可以通过浏览器开发者工具查看网络请求）
2. 在地址栏手动修改 URL 为 `http://localhost:3000/chat?id=<对话 ID>`
3. 按回车刷新页面
4. 观察页面标题

**预期结果:**
- ✅ 标题显示为该对话的现有标题
- ✅ 右侧内容显示该对话的消息历史
- ✅ 左侧该对话被高亮选中

---

### 场景 3: 从左侧列表点击对话

**步骤:**
1. 访问 `http://localhost:3000/chat`
2. 在左侧对话列表中点击任意一个已有对话
3. 观察页面标题变化

**预期结果:**
- ✅ 标题立即更新为该对话的标题
- ✅ 右侧内容切换为该对话的消息
- ✅ URL 可能不会变化（这是正常的，使用状态管理）

---

### 场景 4: 新对话发送第一条消息 → 标题自动更新

**步骤:**
1. 访问 `http://localhost:3000/chat`（确保是新对话）
2. 在输入框中输入一个问题，例如："我最近经常头疼，可能是什么原因？"
3. 点击发送或按 Enter
4. 等待 AI 回复完成
5. 观察：
   - 左侧对话列表中的标题
   - 右侧顶部的页面标题

**预期结果:**
- ✅ 左侧对话列表中的标题更新为 "我最近经常头疼，可能是什么原因？"（前 50 字符）
- ✅ 右侧顶部标题同步更新
- ✅ 如果消息超过 50 字符，标题会截断并添加 "..."
- ✅ 如果消息末尾有标点，会被去除

**标题生成示例:**
| 原始消息 | 生成的标题 |
|---------|----------|
| "我最近经常头疼，可能是什么原因？" | "我最近经常头疼，可能是什么原因" |
| "这是一个非常长的问题，用于测试标题截断功能，看看是否会正确地截取前 50 个字符并添加省略号" | "这是一个非常长的问题，用于测试标题截断功能，看..." |
| "头疼" | "头疼" |

---

### 场景 5: 已有对话发送新消息 → 标题不改变

**步骤:**
1. 选择一个已经有标题的对话（不是自动创建的默认标题）
2. 发送一条新消息
3. 观察标题是否变化

**预期结果:**
- ✅ 标题保持不变（不会覆盖用户自定义的标题）
- ✅ 消息正常发送和显示

---

### 场景 6: 标题更新失败 → 消息正常发送

**步骤:**
1. 打开浏览器开发者工具（F12）
2. 访问 `http://localhost:3000/chat`（新对话）
3. 发送一条消息
4. 在网络标签中，找到 `updateConversation` 请求
5. 右键该请求 → "Block request URL"（阻止请求）
6. 再次发送一条新消息（需要创建新对话）

**预期结果:**
- ✅ 消息正常发送和显示
- ✅ 标题保持默认值（不会显示错误）
- ✅ 控制台有错误日志：`"更新对话标题失败:"`
- ✅ 不影响用户体验

---

## 🔍 验证方法

### 1. 检查页面标题
直接在浏览器顶部导航栏观察标题显示

### 2. 检查左侧列表标题
观察左侧对话列表中的对话标题是否同步更新

### 3. 检查 API 调用
打开浏览器开发者工具 → Network（网络）标签：
- 发送消息后，应该看到 `POST /api/conversations/{id}/messages/stream` 请求
- 消息完成后，应该看到 `PUT /api/conversations/{id}` 请求（更新标题）

### 4. 检查控制台日志
打开浏览器开发者工具 → Console（控制台）标签：
- 成功时：`"对话标题已更新：{标题}"`
- 跳过时：`"对话已有自定义标题，跳过自动更新"`
- 失败时：`"更新对话标题失败:"`

### 5. 检查 URL 参数
观察地址栏 URL：
- 新对话：`http://localhost:3000/chat`
- 加载对话：`http://localhost:3000/chat?id={对话 ID}`

---

## ⚠️ 注意事项

1. **遵循项目规范**: 禁止自动在浏览器中发送消息测试，需要手动验证
2. **后端服务**: 确保后端服务运行在 `http://localhost:8000`
3. **前端服务**: 确保前端服务运行在 `http://localhost:3000`
4. **清除缓存**: 如果修改后没有生效，尝试硬刷新（Cmd+Shift+R / Ctrl+Shift+F5）
5. **标题长度**: 标题最多 50 个字符，超长会截断并添加 "..."
6. **标点处理**: 标题末尾的标点符号会被自动去除

---

## 🐛 常见问题

### Q1: 标题没有自动更新？
**检查:**
- 是否发送的是第一条消息？（已有消息的对话不会更新标题）
- 对话标题是否为默认标题？（自定义标题不会被覆盖）
- 查看控制台是否有错误日志

### Q2: URL 有 ID 但加载失败？
**检查:**
- 对话 ID 是否正确？
- 该对话是否属于当前用户？
- 查看网络请求是否成功

### Q3: 左侧和右侧标题不一致？
**检查:**
- 是否刷新了页面？
- 检查 `pageTitle` 状态是否正确更新
- 查看是否有多个状态源冲突

---

## ✅ 验收标准

所有测试场景通过后，功能才算完成：

- [ ] 场景 1: 新对话标题显示正确
- [ ] 场景 2: URL ID 加载对话标题正确
- [ ] 场景 3: 左侧点击切换标题同步
- [ ] 场景 4: 第一条消息触发标题更新
- [ ] 场景 5: 已有对话不覆盖标题
- [ ] 场景 6: 更新失败不影响消息发送

---

## 📝 修改记录

| 日期 | 修改内容 | 作者 |
|------|---------|------|
| 2026-09-01 | 初始版本，实现标题动态管理功能 | AI Agent |

---

## 🔗 相关文档

- [规范文档](./specs/chat-page-dynamic-title.md)
- [前端代码](./src/frontend/app/chat/page.tsx)
- [API 文档](./src/frontend/lib/api.ts)
