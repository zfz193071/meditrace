# MediTrace 聊天界面改进规格说明

> 版本：1.0.0  
> 最后更新：2026-08-31  
> 状态：待实现

---

## 问题陈述

MediTrace 聊天界面存在以下用户体验问题：

1. **URL 路径不直观**：当前聊天页面使用 `/conversations` 路径，命名冗长且不符合用户习惯
2. **对话列表标题滚动体验差**：
   - 文字过长时滚动动画在可视区域外消失，用户看不到完整内容
   - 文字能完全显示时仍显示滚动动画，造成视觉干扰
3. **操作菜单显示异常**：鼠标悬停时显示多余的操作按钮，影响界面整洁度

---

## 解决方案

### 1. URL 路径优化
将聊天页面从 `/conversations` 迁移到 `/chat`，提供更简洁、直观的路径。

### 2. 智能滚动动画
实现条件触发的文字滚动动画：
- **触发条件**：仅当文字宽度超过容器宽度时触发
- **滚动行为**：从右向左平滑滚动
- **循环机制**：文字滚出后等待 20px 空白距离后再次从右侧出现
- **静默模式**：文字能完全显示时不触发任何动画

### 3. 操作菜单精简
修复悬停菜单，确保只显示标准的三个操作按钮（重命名、删除、更多）。

---

## 用户故事

### URL 路径优化

1. 作为用户，我希望通过更短的路径访问聊天页面，以便更快地记住和输入 URL
2. 作为用户，我希望聊天页面的 URL 更符合直觉，以便更容易与他人分享
3. 作为用户，我希望旧路径 `/conversations` 能自动跳转到新路径，以便书签仍然有效

### 智能滚动动画

4. 作为用户，当对话标题过长时，我希望看到完整的标题内容滚动显示，以便了解对话主题
5. 作为用户，当对话标题较短时，我希望标题静止显示，避免不必要的动画干扰
6. 作为用户，我希望滚动动画平滑流畅，以便获得良好的视觉体验
7. 作为用户，我希望滚动动画在文字结尾后有适当的空白间隔，以便区分每次滚动循环
8. 作为用户，我希望动画在鼠标移开后停止，以便页面保持静态

### 操作菜单精简

9. 作为用户，当鼠标悬停在对话列表项上时，我希望只显示必要的三个操作按钮，以便界面保持整洁
10. 作为用户，我希望操作按钮的位置和样式一致，以便快速识别和使用
11. 作为用户，我希望不会因为误触操作按钮而意外删除或修改对话

---

## 实现决策

### 模块修改

#### 1. 前端路由重构
- **修改模块**：Next.js 文件系统路由
- **决策**：
  - 将 `src/frontend/app/conversations/` 目录重命名为 `src/frontend/app/chat/`
  - 保留 `conversations` 目录作为重定向页面（301 永久重定向）
  - 更新所有内部链接引用，从 `/conversations` 改为 `/chat`

#### 2. 对话列表项组件
- **修改模块**：`src/frontend/app/chat/page.tsx` 中的对话列表渲染逻辑
- **决策**：
  - 移除内联 `<style>` 标签中的滚动动画定义
  - 使用 CSS 媒体查询和 `scroll-width` 检测实现条件动画
  - 采用 `animation-iteration-count: infinite` 实现循环滚动
  - 添加 `animation-fill-mode: forwards` 确保动画结束时保持正确位置

#### 3. 操作菜单组件
- **修改模块**：`src/frontend/app/chat/page.tsx` 中的菜单渲染逻辑
- **决策**：
  - 检查并删除重复的菜单按钮渲染代码
  - 确保每个对话列表项只显示一套操作菜单
  - 使用条件渲染确保菜单仅在鼠标悬停时显示

### 技术实现细节

#### CSS 滚动动画实现

```css
/* 条件滚动动画：仅当文字溢出时触发 */
.conversation-item:hover .title-text {
  overflow: hidden;
  white-space: nowrap;
}

/* 滚动动画关键帧 */
@keyframes title-scroll {
  0% {
    transform: translateX(100%);
  }
  100% {
    transform: translateX(-100%);
  }
}

/* 应用动画 */
.hover-scroll-title .title-text {
  animation: title-scroll 3s linear infinite;
  animation-play-state: paused; /* 默认暂停 */
}

.hover-scroll-title:hover .title-text {
  animation-play-state: running; /* 悬停时播放 */
}
```

#### 文字溢出检测

```typescript
// 使用 useRef 检测文字是否溢出
const titleRef = useRef<HTMLDivElement>(null);
const [needsScroll, setNeedsScroll] = useState(false);

useEffect(() => {
  const element = titleRef.current;
  if (element) {
    // 检查文字宽度是否超过容器宽度
    const needsScroll = element.scrollWidth > element.clientWidth;
    setNeedsScroll(needsScroll);
  }
}, [title]);
```

### API 合约

- **保持不变**：所有后端 API 接口不受影响
- **前端调用**：更新 API 调用中的路径引用（如有）

### 数据库 Schema

- **无变更**：不涉及数据库结构修改

---

## 测试决策

### 测试标准

1. **功能测试**：验证所有用户故事都能正确实现
2. **视觉测试**：确保动画流畅、无闪烁
3. **兼容性测试**：支持主流浏览器（Chrome、Firefox、Safari、Edge）
4. **回归测试**：确保现有功能不受影响

### 测试模块

#### 1. URL 重定向测试
- 访问 `/conversations` 应自动跳转到 `/chat`
- 浏览器历史记录应正确更新
- 书签和外部链接应仍然有效

#### 2. 滚动动画测试
- 长标题（超过容器宽度）：应显示滚动动画
- 短标题（在容器宽度内）：不应显示滚动动画
- 动画循环：文字滚出后应有 20px 空白间隔
- 鼠标移开：动画应停止

#### 3. 操作菜单测试
- 悬停显示：鼠标悬停时显示三个操作按钮
- 按钮数量：只显示三个按钮（重命名、删除、更多）
- 按钮功能：每个按钮功能正常
- 无重复：不显示多余的按钮

### 先例参考

- 现有测试：`src/frontend/__tests__/` 中的组件测试
- 测试框架：Jest + Testing Library
- 测试模式：参照现有对话页面测试结构

---

## 超出范围

以下内容不在本次规格说明范围内：

1. **后端 API 修改**：不涉及任何后端代码变更
2. **数据库迁移**：不涉及数据结构变更
3. **移动端适配**：现有移动端样式保持不变
4. **国际化支持**：不涉及多语言文本处理
5. **性能优化**：不涉及动画性能优化（如 GPU 加速）
6. **无障碍访问**：不涉及键盘导航和屏幕阅读器支持

---

## 其他说明

### 实现优先级

1. **P0（必须）**：URL 路径修改、滚动动画修复
2. **P1（重要）**：操作菜单精简
3. **P2（可选）**：动画性能优化、浏览器兼容性增强

### 风险评估

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| URL 重定向导致 SEO 影响 | 中 | 使用 301 永久重定向，保留搜索引擎权重 |
| 滚动动画性能问题 | 低 | 使用 CSS transform 而非 position，确保 GPU 加速 |
| 浏览器兼容性问题 | 低 | 使用标准 CSS 动画，主流浏览器均支持 |

### 验收标准

- [ ] 访问 `/chat` 能正常显示聊天页面
- [ ] 访问 `/conversations` 自动跳转到 `/chat`
- [ ] 长标题对话显示平滑滚动动画
- [ ] 短标题对话不显示滚动动画
- [ ] 滚动动画在文字结尾后有 20px 空白间隔
- [ ] 操作菜单只显示三个按钮
- [ ] 所有现有功能正常工作

### 部署注意事项

- 前端 HMR 热更新会自动应用 CSS 修改
- 路由修改需要重新构建（`pnpm build`）
- 建议在开发环境充分测试后再部署到生产环境

---

## 附录

### 相关文件清单

- `src/frontend/app/chat/page.tsx` - 聊天页面主组件（新）
- `src/frontend/app/conversations/page.tsx` - 重定向页面（保留）
- `src/frontend/lib/api.ts` - API 客户端（可能需更新路径引用）
- `src/frontend/app/layout.tsx` - 全局布局（可能需更新导航链接）

### 相关文档

- [SPEC.md](./SPEC.md) - 项目详细规格说明
- [README.md](./README.md) - 项目概述
- [TODO.md](./TODO.md) - 开发任务清单
