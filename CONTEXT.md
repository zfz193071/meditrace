# MediTrace 领域模型

> 本文件定义了 MediTrace 项目的核心领域概念和术语，确保团队对业务语言有一致理解。

---

## 核心概念

### 诊断 (Diagnosis)

**定义**: 系统基于用户输入的症状，通过 AI 引擎生成的医疗建议记录。

**关键属性**:
- **症状 (Symptoms)**: 用户描述的身体健康状况，如"头痛、发烧持续 3 天"
- **诊断建议 (Diagnosis Suggestion)**: AI 识别的可能疾病及其置信度
- **免责声明 (Disclaimer)**: 说明 AI 诊断不能替代专业医疗意见的文本

**边界**:
- 诊断 ≠ 医疗诊断：本系统的输出是**建议**而非**诊断**，不具备法律效力
- 诊断记录一旦上链不可修改，但原始症状数据存储在 IPFS 而非链上

---

### 溯源 (Provenance)

**定义**: 记录诊断数据的来源、处理过程和模型版本，确保可审计性。

**溯源维度**:
| 维度 | 记录内容 | 存储位置 |
|------|---------|---------|
| 数据来源 | 用户输入的症状哈希 | 区块链 |
| 模型版本 | 使用的 AI 模型版本 | 区块链 |
| 时间戳 | 诊断生成时间 | 区块链 |
| 完整报告 | PDF 格式的诊断详情 | IPFS |

**关键概念**:
- **数据哈希 (Data Hash)**: 对诊断数据计算 SHA-256，用于验证完整性
- **链上记录 (On-chain Record)**: 存储在智能合约中的不可篡改元数据
- **链下存储 (Off-chain Storage)**: IPFS 存储完整报告，链上仅存 CID

---

### 模型版本 (Model Version)

**定义**: 标识生成诊断建议的 AI 模型配置，包括模型类型、参数版本、知识库版本。

**版本格式**: `{model-name}-{version}-{knowledge-base-version}`

**示例**: `deepseek-chat-v2.5-medkb-2024q1`

**重要性**:
- 模型更新可能导致诊断结果变化
- 监管审计需要追溯历史诊断使用的模型
- 版本冲突可能影响诊断一致性

---

### 患者身份 (Patient Identity)

**定义**: 用户在系统中的唯一标识，采用区块链地址作为身份凭证。

**设计决策**:
- 使用钱包地址而非邮箱/手机号，保护隐私
- 链上记录仅存储地址哈希，不暴露真实身份
- 用户通过钱包签名证明记录归属

**边界**:
- 身份 ≠ 个人信息：系统不收集真实身份信息
- 身份可匿名，但记录可追溯（通过地址）

---

## 领域关系

### 诊断与溯源的关系

```
诊断 ──[生成]──> 溯源记录
  │                  │
  │                  ▼
  │              区块链 (元数据)
  │                  │
  ▼                  ▼
IPFS (完整报告) <──[CID 引用]── 区块链
```

**关键规则**:
1. 诊断完成后立即生成溯源记录
2. 溯源记录一旦上链不可修改
3. 完整报告存储在 IPFS，链上仅存 CID 哈希

---

### 用户与诊断的关系

```
用户 (Wallet Address)
  │
  │ 拥有
  ▼
┌─────────────────────────┐
│  诊断记录列表           │
│  - 可按时间筛选         │
│  - 可验证链上状态       │
└─────────────────────────┘
```

**关键规则**:
1. 用户只能查看自己的诊断记录
2. 诊断记录与用户地址绑定
3. 历史记录永久可查（只要 IPFS 节点存在）

---

## 业务规则

### BR-01: 诊断生成规则

1. 系统必须返回至少 1 个、最多 5 个可能疾病
2. 每个疾病必须附带置信度（0-1 之间）
3. 每条建议必须包含建议检查项目
4. 所有输出必须附带免责声明

---

### BR-02: 溯源记录规则

1. 诊断完成后 30 秒内必须尝试上链
2. 上链失败需记录日志并标记状态为 `failed`
3. 数据哈希必须使用 SHA-256 算法
4. 模型版本必须在诊断时固定，不可事后修改

---

### BR-03: 隐私保护规则

1. 链上永不存储明文患者数据
2. IPFS 存储的报告需加密（可选）
3. 用户只能查询自己的记录
4. 不提供公开搜索功能

---

## 术语表

| 术语 | 定义 |
|------|------|
| **诊断建议** | AI 生成的可能疾病列表及建议 |
| **溯源** | 记录数据来源和处理过程 |
| **上链** | 将数据写入区块链智能合约 |
| **CID** | Content Identifier，IPFS 内容地址 |
| **RAG** | Retrieval-Augmented Generation，检索增强生成 |
| **数据哈希** | 对数据计算的唯一指纹（SHA-256） |
| **钱包地址** | 区块链用户身份标识 |
| **Gas 费** | 区块链交易手续费 |

---

## 边界与限制

### 系统边界

MediTrace **不**提供：
- ❌ 确诊功能（仅提供建议）
- ❌ 处方开具
- ❌ 紧急医疗响应
- ❌ 患者身份验证（KYC）

MediTrace **提供**：
- ✅ 症状分析建议
- ✅ 诊断溯源记录
- ✅ 历史记录查询
- ✅ 链上验证功能

### 合规边界

- 系统明确声明**不是医疗诊断工具**
- 所有输出附带**免责声明**
- 建议用户**咨询专业医生**
- 不存储真实患者身份信息

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0.0 | 2024 | 初始版本 |

---

## Agent skills

### Issue tracker

GitHub Issues (uses `gh` CLI). See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout. See `docs/agents/domain.md`.
