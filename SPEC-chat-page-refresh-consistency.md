# Chat 页面刷新一致性规范

## Problem Statement

当用户通过 URL 直接访问特定对话页面（如 `/chat/3f5f948e-730c-47a3-b990-0db41a0dc959`）并刷新页面时，左侧侧栏的对话列表不会显示当前激活的对话，导致 UI 状态不一致。用户需要手动点击侧栏中的对话才能看到正确的选中状态。期望的行为是：页面刷新后，左侧菜单应该自动高亮显示当前对话，就像用户在侧栏点击了该对话一样。

## Solution

在 `/chat` 页面（整合后的对话列表 + 详情页面）中，当 URL 包含对话 ID 时，确保：
1. 加载对话列表后自动激活 URL 中指定的对话
2. 激活对话时同步更新侧栏的选中状态（`activeConversation`）
3. 更新浏览器 URL 为动态路由格式 `/chat/{id}`
4. 保持与从侧栏点击对话完全一致的用户体验

## User Stories

1. 作为用户，我可以通过 URL 直接访问特定对话（如 `/chat/abc123`），以便分享对话链接或 bookmark 特定会话
2. 作为用户，当我刷新通过 URL 访问的对话页面时，左侧侧栏应该自动高亮显示当前对话，以便我清楚知道自己在哪个对话中
3. 作为用户，当我刷新页面时，我希望看到与刷新前完全一致的界面状态（侧栏展开/折叠状态、对话内容、滚动位置等），以便无缝继续对话
4. 作为用户，当我通过 URL 访问一个不存在的对话时，系统应该提示我并引导我创建新对话，而不是显示错误页面
5. 作为用户，我希望在侧栏点击对话时，URL 自动更新为动态路由格式（`/chat/{id}`），以便我可以复制链接分享给他人
6. 作为用户，当我新建对话时，URL 应该保持在 `/chat`（无 ID），以便我清楚知道这是新会话
7. 作为用户，我希望页面标题（浏览器标签页）与当前对话标题同步更新，以便在多标签页中快速识别对话内容
8. 作为用户，我希望在移动端刷新页面后，侧栏默认折叠，以便获得更好的移动体验
9. 作为用户，我希望切换对话时消息列表自动滚动到底部，但发送新消息时才触发滚动，避免切换时丢失阅读位置
10. 作为用户，我希望临时会话在发送第一条消息时自动转换为正式会话，以便我的对话记录被持久化保存
11. 作为用户，我希望对话标题在发送第一条消息后自动更新为基于消息内容的智能标题，以便更容易识别对话主题
12. 作为用户，我希望在对话列表中长标题能够自动滚动显示，以便看到完整的标题内容
13. 作为用户，我希望通过三点菜单可以重命名或删除对话，以便管理我的对话记录
14. 作为用户，我希望点击侧栏外部或按 ESC 键可以关闭三点菜单，以便快速关闭菜单
15. 作为用户，我希望在对话详情页面可以看到返回按钮和主页按钮，以便快速导航

## Implementation Decisions

### 架构决策
- **单页面整合**: `/chat` 页面同时承载对话列表和对话详情功能，通过 URL 参数（`params.id`）判断是否加载特定对话
- **URL 同步机制**: 使用 `window.history.pushState` 在用户点击侧栏对话时更新 URL 为动态路由格式
- **状态管理**: 使用 `activeConversation` 状态统一管理当前激活的对话，无论是从列表选择还是从 URL 加载

### 关键实现逻辑

#### 1. URL 对话加载流程
```
页面加载 → 检查 params.id → 有 ID: 加载列表并查找对话 → 找到：激活对话
                                              ↓ 未找到：创建新对话
                              无 ID: 加载列表并自动创建临时对话
```

#### 2. 对话激活逻辑
- 从 URL 加载的对话：在 `loadConversations` 中查找并设置 `activeConversation`
- 从侧栏点击的对话：在 `onClick` 中设置 `activeConversation` 并更新 URL
- 新建对话：创建临时对话，URL 保持在 `/chat`

#### 3. 临时会话转换
- 临时会话 ID 格式：`temp-{timestamp}-{random}`
- 发送第一条消息前检测是否为临时会话
- 如果是，先调用 `convertTempConversationToPermanent` 转换为正式会话
- 转换成功后使用新 ID 发送消息

#### 4. 标题自动生成
- 触发时机：第一条用户消息发送后，AI 第一个 chunk 到达时
- 短消息（≤10 字符）：直接使用消息内容作为标题
- 长消息（>10 字符）：调用后端 AI 生成智能标题
- 防重复机制：使用 `titleGenerationRef` 确保只生成一次

### 修改的文件
- `src/frontend/app/chat/page.tsx` - 整合对话列表和详情功能，添加 URL 对话加载逻辑

### API 接口
- `GET /api/conversations/{patientId}` - 获取对话列表
- `GET /api/conversations/{conversationId}` - 获取特定对话
- `POST /api/conversations` - 创建新对话
- `PUT /api/conversations/{conversationId}` - 更新对话（包括标题）
- `POST /api/chat/stream` - 流式聊天接口
- `POST /api/generate-title` - 生成智能标题

## Testing Decisions

### 测试标准
- 只测试外部用户可见的行为，不测试内部实现细节
- 测试应该覆盖正常流程和边界情况
- 优先使用代码现有的测试模式和工具

### 测试场景

#### 1. URL 对话加载测试
- 访问 `/chat/{existingId}` - 应该加载对应对话并高亮侧栏
- 访问 `/chat/{nonExistentId}` - 应该提示并创建新对话
- 访问 `/chat`（无 ID） - 应该自动创建临时对话

#### 2. 页面刷新一致性测试
- 在对话 A 中刷新页面 - 侧栏应该高亮对话 A
- 在移动端刷新 - 侧栏应该默认折叠
- 刷新后对话内容应该完整保留

#### 3. URL 同步测试
- 点击侧栏对话 - URL 应该更新为 `/chat/{id}`
- 新建对话 - URL 应该保持在 `/chat`
- 复制 URL 在新标签页打开 - 应该加载相同对话

#### 4. 临时会话转换测试
- 新建对话（临时）- ID 应该以 `temp-` 开头
- 发送第一条消息 - 应该先转换为正式会话
- 转换后 URL 应该更新为正式 ID

#### 5. 标题生成测试
- 发送短消息（≤10 字符）- 标题应该直接使用消息内容
- 发送长消息（>10 字符）- 标题应该调用 AI 生成
- 已有自定义标题 - 不应该自动更新

#### 6. 响应式测试
- 移动端（<768px）- 侧栏默认折叠，点击展开
- 桌面端 - 侧栏默认展开
- 窗口大小变化 - 应该自动切换模式

### 现有测试参考
- 查看 `src/frontend/__tests__/` 目录中的现有测试模式
- 参考 `conversation` 相关测试的测试结构和断言方式

## Out of Scope

1. **后端代码修改**: 当前需求仅涉及前端 URL 同步和状态管理，后端 API 已经支持
2. **对话列表排序**: 不涉及对话列表的排序逻辑优化
3. **对话搜索/过滤**: 不涉及在对话列表中搜索或过滤功能
4. **多用户支持**: 当前使用固定的 `userId`，不涉及多用户切换
5. **离线支持**: 不涉及 Service Worker 或离线缓存机制
6. **对话导出/导入**: 不涉及对话的备份和恢复功能
7. **消息编辑/删除**: 只支持发送新消息，不支持编辑或删除已有消息
8. **附件上传**: 不支持图片、文件等附件上传功能

## Further Notes

### 技术细节

#### Next.js 路由结构
```
/chat                 → 对话列表 + 新建临时对话
/chat/{id}           → 对话列表 + 加载特定对话
/chat/[id]/page.tsx  → 独立的对话详情页面（已废弃，功能整合到 /chat/page.tsx）
```

#### 状态同步关键点
1. `useEffect` 监听 `params.id` - 处理 URL 变化
2. `activeConversation` 状态 - 统一控制当前对话
3. `pageTitle` 状态 - 同步页面标题和浏览器标签
4. `isTempConversation` 状态 - 标记临时/正式会话

#### 性能优化
- 标题滚动动画使用 CSS animation + `animation-play-state` 实现，避免 JavaScript 性能开销
- 防重复创建使用 `isAutoCreating` flag 和 `useRef` 组合
- 标题生成使用 `useRef` 而非 `useState` 避免不必要的重渲染

#### 已知限制
- 临时会话在页面卸载前未发送消息会被清理（组件卸载时的 `useEffect`）
- URL 更新使用 `pushState` 而非 Next.js `router.push`，避免不必要的页面重渲染
- 移动端侧栏使用 `fixed` 定位，点击遮罩层关闭

### 验证步骤（手动测试）
1. 启动前端服务：`cd src/frontend && pnpm dev`
2. 访问 `http://localhost:3000/chat` - 应该看到对话列表和新建的临时对话
3. 发送一条消息 - 应该转换为正式会话，URL 更新为 `/chat/{id}`
4. 复制 URL 在新标签页打开 - 应该看到相同的对话，侧栏高亮
5. 刷新页面 - 侧栏应该保持高亮状态
6. 点击侧栏其他对话 - URL 应该更新，侧栏高亮切换
7. 在移动端刷新 - 侧栏应该默认折叠
