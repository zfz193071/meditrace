# 实时智能标题更新规范

## Problem Statement

当前 MediTrace 聊天页面在标题管理方面存在以下用户体验问题：

1. **标题更新延迟**：会话标题仅在 AI 回复完成后才更新，用户无法在 AI 生成回复的过程中看到标题的变化
2. **标题更新时机不合理**：标题更新基于"第一条消息"，但在多轮对话场景中，用户可能先看到默认标题"新的诊断对话"，需要等待整个回复完成才能看到有意义的标题
3. **缺乏实时反馈**：当 AI 开始生成回复时，左侧会话列表和页面标题仍然显示默认标题，无法给用户即时的上下文反馈
4. **标题与内容不同步**：在流式回复过程中，用户无法通过标题快速了解当前对话的主题

## Solution

实现实时智能标题更新功能，在 AI 开始回复时立即提取并更新会话标题：

1. **实时标题提取**：当 AI 开始生成第一条回复时，立即基于用户的问题生成智能标题
2. **双向同步更新**：同时更新页面标题、左侧会话列表中的标题、以及 URL 中的标题参数
3. **智能标题生成**：结合用户问题和 AI 回复的上下文，生成更准确的标题
4. **渐进式标题优化**：在 AI 回复过程中，标题可以随着内容的增加而优化（可选）

## User Stories

### 核心功能

1. 作为用户，当我发送问题后 AI 开始回复时，我希望标题立即从"新的诊断对话"变成我问题的主题，这样我可以快速了解当前对话的上下文
2. 作为用户，我希望标题在 AI 开始生成第一个字时就更新，而不是等待回复完成，这样我能获得即时的反馈
3. 作为用户，我希望左侧会话列表中的标题和页面顶部的标题同步更新，这样无论从哪个位置都能看到一致的标题
4. 作为用户，我希望标题能够准确反映我问题的核心内容（例如"头痛诊断建议"而不是"头疼"），这样我可以快速识别对话主题
5. 作为用户，当标题更新时，我希望浏览器标签页也同步更新，这样在多标签浏览时能轻松识别每个标签的内容

### 标题生成质量

6. 作为用户，当我输入简短问题时（如"头疼"），我希望标题直接使用我的问题，这样标题简洁明了
7. 作为用户，当我输入长问题时（如详细描述症状），我希望 AI 提取核心主题作为标题（如"持续性头痛伴恶心症状诊断"），这样标题既有信息量又简洁
8. 作为用户，当我的问题包含多个症状时，我希望标题能够提取最主要的症状作为标题，这样标题聚焦且清晰
9. 作为用户，当 AI 生成标题失败时，我希望系统使用备用方案（截断我的问题前 30 个字符），这样标题永远不会为空
10. 作为用户，我希望标题不包含标点符号和多余的空白，这样标题整洁专业

### 会话列表体验

11. 作为用户，当标题更新后，我希望左侧会话列表中的标题立即变化，这样我不需要刷新就能看到最新标题
12. 作为用户，当会话列表中的标题过长时，我希望标题可以滚动显示完整内容，这样我可以看到完整的标题
13. 作为用户，当我在会话列表中切换对话时，我希望标题更新不会干扰我的浏览，这样我可以流畅地查看历史对话
14. 作为用户，当标题更新后，我希望当前激活的会话在列表中保持高亮状态，这样我知道自己正在查看哪个对话
15. 作为用户，当我刷新页面后，我希望之前生成的标题会被保留，这样我不需要重新生成标题

### 多轮对话场景

16. 作为用户，当我在已有标题的对话中继续提问时，我希望标题保持不变，这样对话的连续性不会被破坏
17. 作为用户，当对话已有自定义标题时，我希望系统不再自动更新标题，这样我可以保持自己设置的标题
18. 作为用户，当 AI 正在生成标题时，我希望不会阻塞消息的发送和显示，这样用户体验流畅
19. 作为用户，当标题生成失败时，我希望消息发送不受影响，这样功能更加健壮

### 性能体验

20. 作为用户，我希望标题生成是异步进行的，不会阻塞 AI 回复的流式显示，这样我不会感觉到延迟
21. 作为用户，当标题生成需要较长时间时，我希望先显示默认标题，等生成完成后再更新，这样不会看到空白或加载中状态
22. 作为用户，我希望标题更新是平滑的，不会有明显的闪烁或跳动，这样视觉体验更好

### 错误处理

23. 作为用户，当网络不稳定导致标题生成失败时，我希望有降级方案（使用问题摘要），这样标题永远不会为空
24. 作为用户，当后端 API 超时或返回错误时，我希望前端有友好的错误处理，不会影响对话功能
25. 作为用户，当标题生成服务不可用时，我希望系统自动回退到本地标题生成逻辑，这样功能依然可用

### 移动端体验

26. 作为用户，在移动端设备上，我希望标题更新后的显示效果与桌面端一致，这样体验统一
27. 作为用户，当在移动端折叠侧边栏时展开时，我希望会话列表中的标题已经更新，这样我看到的是最新状态

## Implementation Decisions

### 前端实现

#### 标题更新时机
- **触发条件**：当 AI 消息（assistant 角色）开始接收流式数据时（第一个 chunk 到达）
- **更新位置**：
  - 页面顶部标题 (`pageTitle`)
  - 左侧会话列表中的标题 (`conversations` 状态)
  - 当前激活会话的标题 (`activeConversation` 状态)
  - 浏览器标签页标题 (`document.title`)

#### 标题生成策略
- **短消息处理**（≤10 字符）：
  - 直接使用用户消息，去除末尾标点
  - 示例："头疼" → "头疼"
  
- **长消息处理**（>10 字符）：
  - 调用后端 `/api/generate-title` API
  - Prompt 优化：结合用户问题和 AI 回复的开头内容
  - 限制标题长度在 20-30 字符
  
- **降级策略**：
  - API 失败时使用本地截断逻辑
  - 截断前 30 个字符并添加省略号

#### 异步处理
- **不阻塞主流程**：标题生成在后台异步进行，不影响消息发送和显示
- **并发控制**：避免重复调用标题生成 API（使用防抖或状态标记）
- **错误隔离**：标题生成失败不影响对话功能

#### 状态管理
- **新增状态**：
  - `isGeneratingTitle`: 标记是否正在生成标题
  - `titleGenerationFailed`: 标记标题生成是否失败
  
- **现有状态复用**：
  - `pageTitle`: 页面标题
  - `activeConversation`: 当前会话（包含标题）
  - `conversations`: 会话列表（包含各会话标题）

#### UI 更新机制
- **实时同步**：标题生成完成后，同时更新多个状态源
- **防闪烁**：在标题生成期间保持原标题，完成后一次性更新
- **平滑过渡**：使用 CSS 过渡效果（可选）

### 后端实现

#### API 优化
- **现有接口复用**：使用 `/api/generate-title` 端点
- **Prompt 增强**：
  - 输入：用户问题 + AI 回复开头（可选）
  - 输出：简洁的中文标题（20-30 字符）
  - 示例 Prompt：
    ```
    请根据以下用户问题和 AI 回复，生成一个简洁的对话标题（20-30 个中文字符）：
    
    用户问题：{user_message}
    AI 回复开头：{ai_response_preview}
    
    要求：
    1. 标题应准确反映对话的核心主题
    2. 标题简洁明了，不超过 30 个字符
    3. 使用中文，不要使用标点符号
    4. 优先提取用户问题的关键症状或主题
    
    请直接返回标题内容。
    ```

#### 性能优化
- **缓存机制**：对常见问题标题生成结果进行缓存
- **超时控制**：设置合理的超时时间（如 5 秒）
- **降级处理**：超时或失败时返回原始消息截断

### 数据流

```
用户发送消息
    ↓
前端添加用户消息到对话
    ↓
前端添加空的 AI 消息占位符
    ↓
AI 开始流式返回（第一个 chunk）
    ↓
触发标题生成逻辑（异步）
    ↓
调用 /api/generate-title
    ↓
后端调用 LLM 生成标题
    ↓
返回标题给前端
    ↓
前端更新多个状态源：
  - pageTitle
  - activeConversation.title
  - conversations[].title
  - document.title
```

### 代码修改点

#### 前端文件
- `src/frontend/app/chat/page.tsx`:
  - 在 `handleSendMessage` 的流式数据回调中触发标题生成
  - 新增 `generateTitleOnFirstChunk` 函数
  - 更新状态同步逻辑

#### 后端文件
- `src/backend/main.py`:
  - 优化 `/api/generate-title` 的 Prompt
  - 可选：添加标题缓存机制

## Testing Decisions

### 测试策略

#### 功能测试
- **测试重点**：验证标题在正确时机生成和更新
- **测试场景**：
  - AI 开始回复时标题立即更新
  - 标题更新同步到所有位置（页面标题、侧边栏、浏览器标签）
  - 短消息和长消息的标题生成差异
  - 已有标题的对话不再更新标题
  
#### 边界测试
- **测试场景**：
  - 空消息或仅包含标点
  - 超长消息（>500 字符）
  - 特殊字符和 emoji
  - 多语言混合输入
  - 网络超时或 API 失败

#### 性能测试
- **测试场景**：
  - 标题生成不阻塞消息流式显示
  - 快速连续发送多条消息时的标题生成
  - 标题生成失败后的降级处理

### 手动测试步骤

1. **基本功能测试**：
   - 打开 `/chat` 页面
   - 发送一个问题（如"我最近经常头痛"）
   - 观察 AI 开始回复时标题是否立即更新
   - 检查页面标题、侧边栏标题、浏览器标签是否同步

2. **标题质量测试**：
   - 发送短问题："头疼"
   - 验证标题为"头疼"
   - 发送长问题："我最近三天持续头痛，伴有恶心和畏光，可能是偏头痛吗？"
   - 验证标题为 AI 生成的摘要（如"持续头痛伴恶心畏光症状诊断"）

3. **错误处理测试**：
   - 断开网络或停止后端服务
   - 发送消息
   - 验证标题使用降级方案（问题截断）
   - 恢复服务后验证功能正常

4. **多轮对话测试**：
   - 创建新对话并发送第一条消息
   - 等待标题生成完成
   - 继续发送第二条消息
   - 验证标题不再更新

5. **会话列表测试**：
   - 创建多个对话并生成标题
   - 在不同对话间切换
   - 验证每个对话的标题正确显示
   - 刷新页面后验证标题被保留

## Out of Scope

以下功能不在本次规范范围内：

1. **标题编辑功能**：用户手动编辑生成的标题（已有重命名功能）
2. **标题历史版本**：记录标题的修改历史
3. **标题推荐列表**：提供多个标题供用户选择
4. **标题翻译**：支持多语言标题
5. **标题分析**：分析标题的质量或相关性
6. **批量标题优化**：对历史对话批量重新生成标题
7. **标题模板**：预设标题模板或分类
8. **标题搜索优化**：基于标题的搜索引擎优化

## Further Notes

### 技术注意事项

1. **API 调用时机**：
   - 标题生成应该在 AI 回复的第一个 chunk 到达时触发
   - 使用防抖机制避免重复调用（如 500ms 内只调用一次）
   - 确保只针对第一条用户消息生成标题

2. **状态同步**：
   - 更新标题时需要同时更新多个状态源
   - 使用 React 的批量更新避免多次渲染
   - 注意避免无限循环更新

3. **错误处理**：
   - 标题生成失败不应影响对话功能
   - 使用 try-catch 包裹 API 调用
   - 提供清晰的降级方案

4. **性能考虑**：
   - 标题生成是异步的，不应阻塞 UI
   - 考虑添加标题生成超时控制
   - 对标题生成结果进行缓存（可选）

### 用户体验优化

1. **视觉反馈**：
   - 标题更新时可以有轻微的动画效果
   - 避免标题频繁跳动或闪烁
   - 保持标题长度一致性的视觉效果

2. **标题质量**：
   - Prompt 设计要确保标题的准确性和简洁性
   - 测试不同场景下的标题生成效果
   - 根据用户反馈持续优化标题生成逻辑

3. **国际化**：
   - 当前实现针对中文优化
   - 如需支持多语言，需要调整 Prompt 和长度限制

### 后续扩展

1. **智能标题分类**：根据标题自动分类对话（如"头痛"、"发烧"、"消化系统"等）
2. **标题标签**：为标题添加标签或关键词
3. **标题分享**：支持通过标题快速分享对话
4. **标题统计**：统计常用标题类型，优化诊断流程
