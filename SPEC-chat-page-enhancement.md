# Chat Page Enhancement Specification

## Problem Statement

当前 MediTrace 聊天页面存在以下用户体验问题：

1. **无效会话占用**：每次新会话都会立即保存到数据库，即使用户没有输入任何内容，导致侧边栏充斥大量空会话
2. **标题缺乏上下文**：新会话的标题固定为"新的诊断对话"，无法反映用户实际讨论的内容，难以识别和查找历史对话
3. **阅读体验不佳**：AI 回复区域宽度受限（max-w-[70%]），带有背景色，且流式回复时滚动条不会自动跟随，影响阅读体验

## Solution

实现智能会话管理和优化的聊天体验：

1. **延迟保存会话**：新创建的会话仅在用户发送第一条消息后才真正保存到数据库
2. **动态标题生成**：基于用户第一条消息自动生成有意义的会话标题，过长时由 AI 智能提取关键内容
3. **优化回复展示**：AI 回复全宽度显示、去除背景色、流式回复时自动滚动（用户未手动查看历史消息时）

## User Stories

### 会话管理

1. 作为用户，当我首次打开聊天页面时，我希望系统自动创建一个临时会话，但不会立即占用我的会话列表空间，这样我可以先看看界面再决定是否使用
2. 作为用户，当我新建会话但没有发送任何消息就关闭页面时，我希望这个空会话不会被保存，这样我的会话列表不会被无用记录污染
3. 作为用户，当我发送第一条消息后，我希望会话被正式保存到数据库，这样我的对话记录不会被丢失
4. 作为用户，当我删除一个空会话时，我希望它直接从数据库中移除，不会有残留记录
5. 作为用户，当我刷新页面时，我希望只有已发送消息的会话会被保留，临时会话会被清除，这样我不会看到重复的空会话

### 标题管理

6. 作为用户，当我发送第一个问题时，我希望会话标题自动变成我的问题摘要，这样我可以快速识别这个对话的内容
7. 作为用户，当我输入一个很长的问题时，我希望 AI 智能提取关键内容作为标题（例如 20-30 个字符），这样标题简洁且有信息量
8. 作为用户，当会话标题过长时，我希望侧边栏中的标题可以滚动显示完整内容，这样我可以看到完整的标题信息
9. 作为用户，当页面标题与当前会话标题同步更新时，我希望浏览器标签页显示正确的对话标题，这样在多标签浏览时易于识别
10. 作为用户，当我手动重命名对话时，我希望系统保留我的自定义标题，不再被自动覆盖，这样我可以按照自己的习惯组织对话
11. 作为用户，当我查看历史对话列表时，我希望每个对话的标题都能反映其核心内容，这样我可以快速找到需要的对话
12. 作为用户，当我发送第一条消息后，我希望标题更新是异步进行的，不会影响消息发送的体验，这样我不会感觉到延迟

### 回复展示

13. 作为用户，当 AI 回复内容时，我希望回复区域占据整个可用宽度，而不是被限制在 70%，这样我可以充分利用屏幕空间阅读长内容
14. 作为用户，当 AI 回复内容时，我希望去掉背景色，使用更简洁的纯文本展示，这样阅读体验更清爽
15. 作为用户，当 AI 逐字回复时，我希望滚动条自动跟随到新内容，这样我不会错过任何回复内容
16. 作为用户，当我手动向上滚动查看历史消息时，AI 继续回复，我希望滚动条保持在我查看的位置，而不是强制跳到底部，这样我可以继续阅读历史内容
17. 作为用户，当我点击左侧菜单中的某个历史对话时，我希望消息列表滚动到该对话的初始位置，而不是强制跳到底部，这样我可以保持我的阅读位置
18. 作为用户，当 AI 回复完成时，我希望最后一条消息清晰可见，不会有内容被遮挡，这样我可以完整看到 AI 的回复
19. 作为用户，当消息数量增加时（包括流式更新），我希望只有在新消息到达时才自动滚动，而不是每次字符更新都滚动，这样不会因为频繁滚动而干扰阅读
20. 作为用户，当切换对话时，我希望滚动位置被重置，这样新对话从顶部开始显示

### 用户体验

21. 作为用户，当我在聊天过程中切换窗口或最小化浏览器，AI 继续回复，我希望回到页面时能看到完整的回复内容，不会有显示异常
22. 作为用户，当我的网络不稳定导致消息发送失败时，我希望有明确的错误提示，并且可以重试，这样我知道发生了什么
23. 作为用户，当 AI 正在回复时，我希望发送按钮被禁用，这样我不会误发多条消息
24. 作为用户，当我输入问题时，我希望可以通过回车键快速发送，这样操作更便捷
25. 作为用户，当侧边栏在移动端打开时，我希望点击遮罩层可以关闭侧边栏，这样操作更直观

## Implementation Decisions

### 前端实现

#### 会话生命周期管理
- **临时会话状态**：引入 `isTempConversation` 状态标记，标识当前会话是否为临时会话
- **延迟持久化**：在 `handleSendMessage` 中，当检测到是临时会话且为第一条消息时，调用新的 API 端点将临时会话转换为正式会话
- **会话清理**：在组件卸载时，如果当前会话是临时会话且没有消息，调用删除 API 清理临时会话

#### 智能标题生成
- **标题提取策略**：
  - 短消息（≤50 字符）：直接使用消息内容，去除末尾标点
  - 长消息（>50 字符）：调用 AI 服务提取核心主题作为标题
- **标题更新时机**：仅在第一条用户消息发送后触发标题更新，后续消息不影响标题
- **标题去重**：避免与现有对话标题重复，必要时添加时间戳后缀

#### 滚动行为优化
- **智能滚动控制**：
  - 使用 `shouldAutoScroll` 状态追踪用户是否主动滚动
  - 仅在 `shouldAutoScroll = true` 时执行自动滚动
  - 用户手动滚动时设置 `shouldAutoScroll = false`
  - 新消息到达时重置 `shouldAutoScroll = true`
- **流式更新优化**：
  - 在流式数据 chunk 更新时不触发滚动
  - 仅通过消息数量变化的 useEffect 处理滚动
  - 避免每次字符更新都触发滚动动画

#### UI 样式调整
- **消息容器样式**：
  - 移除 AI 消息的 `max-w-[70%]` 限制
  - 移除背景色 `bg-gray-100` 和 `dark:bg-gray-800`
  - 使用透明背景和边框样式
- **响应式适配**：保持移动端和桌面端的响应式布局

### 后端实现

#### 新的 API 端点
- **转换临时会话为正式会话**：
  - `POST /api/conversations/{conversation_id}/convert`
  - 验证会话存在性
  - 检查是否有消息内容
  - 更新会话的 `updated_at` 时间戳
  - 返回转换后的会话信息

#### 标题生成服务
- **AI 标题提取**：
  - 使用现有的 LLM 服务
  - Prompt 设计：提取用户问题的核心主题，限制在 20-30 字符
  - 缓存常见问题的标题生成结果

### 数据流

```
用户操作 → 前端状态更新 → API 调用 → 后端处理 → 数据库更新 → 前端状态同步
```

1. 用户发送第一条消息
2. 前端检测为临时会话
3. 调用 `/convert` 端点转换会话
4. 后端验证并更新数据库
5. 前端更新会话状态和标题
6. 调用标题生成服务
7. 更新对话列表和页面标题

## Testing Decisions

### 测试策略

#### 会话管理测试
- **测试重点**：验证临时会话的创建、转换和清理逻辑
- **测试场景**：
  - 页面加载时临时会话的创建
  - 发送第一条消息后会话的转换
  - 刷新页面后临时会话的清理
  - 删除空会话的功能
- **测试方法**：使用浏览器开发者工具检查数据库状态和网络请求

#### 标题生成测试
- **测试重点**：验证标题提取的准确性和性能
- **测试场景**：
  - 短消息标题生成
  - 长消息标题提取
  - 特殊字符和标点处理
  - 标题去重逻辑
- **测试方法**：手动发送不同长度的消息，观察标题变化

#### 滚动行为测试
- **测试重点**：验证滚动行为的正确性和用户体验
- **测试场景**：
  - 流式回复时的自动滚动
  - 用户手动滚动后的滚动保持
  - 切换对话时的滚动重置
  - 新消息到达时的滚动触发
- **测试方法**：在浏览器中手动测试各种滚动场景

#### UI 样式测试
- **测试重点**：验证样式调整的正确性
- **测试场景**：
  - AI 消息的全宽度显示
  - 背景色的移除
  - 响应式布局的适配
- **测试方法**：在不同屏幕尺寸下检查 UI 样式

### 测试注意事项

1. **禁止自动对话测试**：根据项目规则，禁止自动在浏览器中发送消息进行测试，需要用户手动验证
2. **API Token 保护**：避免消耗 DeepSeek API token，所有涉及 AI 回复的测试都需要手动进行
3. **服务重启**：后端代码修改后需要重启服务，前端修改后 HMR 会自动应用

## Out of Scope

以下功能不在本次规格说明范围内：

1. **多语言标题生成**：仅支持中文和英文标题提取，不支持其他语言
2. **标题编辑历史**：不支持查看和恢复之前的标题版本
3. **批量会话管理**：不支持批量删除、合并或导出会话
4. **会话搜索**：不支持在会话列表中搜索特定内容
5. **离线模式**：不支持离线状态下使用聊天功能
6. **消息编辑和删除**：不支持编辑或删除已发送的消息
7. **语音输入**：不支持语音转文字的输入方式
8. **文件上传**：不支持在对话中上传图片和文档

## Further Notes

### 技术债务

1. **临时会话 ID 生成**：当前使用 UUID 生成临时会话 ID，需要考虑与正式会话 ID 的兼容性和冲突处理
2. **标题生成性能**：AI 标题生成可能需要较长时间，需要考虑加载状态和超时处理
3. **滚动性能优化**：在消息数量较多时，滚动性能可能需要进一步优化（如虚拟列表）

### 已知限制

1. **浏览器兼容性**：某些高级 CSS 特性可能在旧版浏览器中不支持
2. **移动端体验**：移动端的滚动行为可能与桌面端有所不同，需要额外测试
3. **网络延迟**：在网络延迟较高的情况下，会话转换和标题生成可能会有明显延迟

### 未来改进方向

1. **智能会话分组**：根据时间和主题自动分组会话
2. **会话摘要**：为长对话生成摘要
3. **消息搜索**：在对话内容中搜索关键词
4. **导出功能**：支持导出对话记录为 PDF 或文本文件
5. **协作功能**：支持多人共享同一个对话
