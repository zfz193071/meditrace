# Conversations Page UI Enhancement Spec

## Problem Statement

当前 `/conversations` 页面存在以下用户体验问题：

1. **进入页面时无新对话状态**：用户进入页面时需要手动选择一个已有对话或创建新对话，无法直接开始新的医疗咨询
2. **标题展示不清晰**：页面标题没有根据对话状态动态变化，无法区分新对话和已有对话
3. **对话列表展示冗余**：每条对话占用多行空间，包含标题、最后消息内容、日期等信息，占用过多侧栏空间
4. **缺少便捷的对话管理**：删除对话需要直接点击删除按钮，缺少重命名等管理功能
5. **存在冗余头部**：右侧内容区域有两个头部（顶部导航栏 + 对话信息栏），造成空间浪费

## Solution

优化 `/conversations` 页面的用户体验，使其更符合现代 AI 对话应用（如豆包）的使用习惯：

1. **默认进入新对话**：页面加载时自动创建并进入新对话状态
2. **动态标题展示**：根据对话状态动态调整页面标题
3. **紧凑的对话列表**：每条对话仅用一行展示，支持滚动效果和快捷操作
4. **右键菜单管理**：通过三点菜单提供重命名、删除等管理功能
5. **精简头部区域**：移除冗余的对话信息头部

## User Stories

1. 作为用户，我希望进入 `/conversations` 页面时直接进入新对话状态，这样我可以立即开始医疗咨询而无需额外操作
2. 作为用户，我希望新对话时页面标题显示"MediTrace 对话"，这样我能清楚知道当前处于新建对话状态
3. 作为用户，我希望选择已有对话时页面标题显示"有效提取用户的问题"，这样我能清楚知道当前处于历史对话状态
4. 作为用户，我希望对话列表每条仅占一行，这样我可以在有限空间内看到更多对话记录
5. 作为用户，我希望对话列表左侧显示标题、右侧显示时间，这样我能快速获取对话的关键信息
6. 作为用户，我希望鼠标移入对话时标题能够自左向右滚动，这样当标题过长时我仍能完整看到内容
7. 作为用户，我希望鼠标移入对话时时间显示变为"..."，这样我能知道有更多操作可用
8. 作为用户，我希望点击"..."后出现右下弹框，这样我能方便地访问对话管理功能
9. 作为用户，我希望在弹框中能够重命名对话，这样我能更好地组织和管理我的医疗咨询记录
10. 作为用户，我希望在弹框中能够删除对话，这样我能清理不再需要的咨询记录
11. 作为用户，我希望移除右侧内容区域的第二个头部（对话信息栏），这样我能获得更大的聊天内容展示空间
12. 作为用户，我希望页面整体风格像豆包等现代 AI 对话应用，这样我能获得熟悉和流畅的使用体验

## Implementation Decisions

### 修改的模块

- **前端页面组件**: `src/frontend/app/conversations/page.tsx`
  - 修改页面初始状态逻辑，默认创建新对话
  - 实现动态标题展示逻辑
  - 重构对话列表项的展示样式和功能
  - 添加三点菜单和弹框组件
  - 移除冗余的对话信息头部

### 技术决策

1. **自动创建对话逻辑**:
   - 使用 `useEffect` 在组件挂载时自动调用 `createConversation`
   - 添加状态标记防止重复创建
   - 处理创建失败的异常情况

2. **动态标题逻辑**:
   - 新对话（刚创建，无历史消息）: 显示 "MediTrace 对话"
   - 已有对话（从列表选择）: 显示 "有效提取用户的问题"
   - 使用条件渲染根据 `activeConversation` 状态和消息数量判断

3. **对话列表项重构**:
   - 单行展示：标题（左侧）+ 时间（右侧）
   - 添加 CSS 动画实现标题滚动效果（`@keyframes scroll`）
   - 鼠标悬停时切换时间显示为 "..."
   - 使用绝对定位实现右下弹框

4. **三点菜单功能**:
   - 点击 "..." 显示弹框（重命名、删除）
   - 弹框位置：对话列表项的右下角
   - 点击外部区域关闭弹框
   - 重命名功能：调用后端更新对话标题接口
   - 删除功能：保持现有的删除逻辑

5. **移除冗余头部**:
   - 删除 `<div className="p-4 border-b bg-gray-50 flex-shrink-0">` 元素
   - 保留顶部导航栏（侧栏折叠按钮、返回主页按钮、页面标题）

### 样式决策

- 使用 Tailwind CSS 实现响应式布局
- 标题滚动动画使用 CSS `@keyframes`
- 弹框使用固定定位，带阴影和圆角
- 保持现有的绿色主题色和整体设计风格

## Testing Decisions

### 测试范围

1. **页面初始化测试**:
   - 验证进入页面时自动创建新对话
   - 验证新对话标题显示为 "MediTrace 对话"
   - 验证页面直接进入聊天状态，无需选择对话

2. **标题动态变化测试**:
   - 验证新对话状态标题正确
   - 验证选择已有对话后标题变为 "有效提取用户的问题"
   - 验证切换回新对话时标题正确更新

3. **对话列表展示测试**:
   - 验证每条对话仅占一行
   - 验证标题和时间位置正确
   - 验证长标题滚动效果正常
   - 验证鼠标悬停时时间变为 "..."

4. **三点菜单功能测试**:
   - 验证点击 "..." 显示弹框
   - 验证弹框包含重命名和删除选项
   - 验证重命名功能正常工作
   - 验证删除功能正常工作
   - 验证点击外部区域关闭弹框

5. **UI 布局测试**:
   - 验证第二个头部已被移除
   - 验证聊天内容区域空间增大
   - 验证整体布局符合现代 AI 对话应用风格

### 测试方法

- **手动测试**: 由于项目规则禁止自动在浏览器发起对话测试，所有 UI 交互测试需手动验证
- **代码审查**: 检查代码实现是否符合设计规范
- **视觉验证**: 检查滚动效果、弹框样式等视觉效果

## Out of Scope

1. **对话重命名的后端实现**: 需要确认后端是否支持更新对话标题的接口
2. **对话搜索和过滤功能**: 本次需求不涉及
3. **对话归档或分类功能**: 本次需求不涉及
4. **多语言支持**: 保持现有的中文界面
5. **移动端适配优化**: 保持现有的响应式逻辑

## Further Notes

### 注意事项

1. **API 依赖**: 
   - 需要确认后端是否提供更新对话标题的 API（`PUT /api/conversations/{id}`）
   - 如果不存在，需要后端先实现该接口

2. **性能考虑**:
   - 自动创建对话应在组件挂载时执行一次，避免重复创建
   - 滚动动画应使用 CSS transform 而非 width/height 变化，保证性能

3. **用户体验**:
   - 弹框应支持 ESC 键关闭
   - 重命名支持回车键确认
   - 删除操作应有二次确认

4. **与现有功能的兼容性**:
   - 保持现有的消息发送、流式响应等功能不变
   - 保持现有的侧栏折叠/展开功能不变
   - 保持现有的移动端适配逻辑不变

### 参考设计

- **豆包（Doubao）对话界面**: 参考其对话列表的紧凑展示和三点菜单设计
- **现代 AI 聊天应用**: 如 ChatGPT、Claude 等的对话管理方式

### 修改文件清单

- `src/frontend/app/conversations/page.tsx` - 主要修改文件

### 预计影响

- **前端**: 中等影响，主要涉及 UI 重构和状态逻辑调整
- **后端**: 无影响（除非需要新增重命名 API）
- **数据库**: 无影响
